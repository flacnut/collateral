import React from "react";
import { Container } from "@material-ui/core";

type Props = {
  accountId: number;
};

export default function AccountView(props: Props) {
  return <Container>An Account</Container>;
}
